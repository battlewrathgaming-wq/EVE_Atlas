No routine doc updates for normal implementation details.
Docs only when direction/terms/contracts change, or when you explicitly ask.
Audit docs only during specific audit activity, not as a reflex after every fix.
For regular code slices, I’ll report what changed in the final response and rely on tests unless the change creates a durable doctrine/schema/definition shift.